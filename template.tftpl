all:
  hosts:
    my_host:
      ansible_host: ${ip}
      ansible_connection: ssh
      ansible_port: 22
      ansible_user: ${login}
    vars:
      ansible_ssh_private_key_file: "/home/user/.ssh/id_rsa"
